const state={view:'overview',items:[],stats:{},health:{}};const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(path,opts={}){const r=await fetch(path,opts);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}function note(m){const n=document.createElement('div');n.className='notice';n.textContent=m;document.body.append(n);setTimeout(()=>n.remove(),2500)}function shell(x){return `<div class="shell"><aside class="sidebar"><div class="brand"><span class="mark"><span>in</span><strong>my</strong></span>InMyArtifact</div><div class="nav">${['overview','artifacts','upload'].map(v=>`<button data-view="${v}" class="${state.view===v?'active':''}">${v[0].toUpperCase()+v.slice(1)}</button>`).join('')}</div><div class="side-note"><strong>Opaque bytes only.</strong><br>Store · hash · verify · deduplicate. Never extract or execute.</div></aside><main class="main"><header class="topbar"><strong>Content-addressed storage</strong><small>127.0.0.1 · standalone · ecosystem off</small></header><section class="content">${x}</section></main></div>`}function overview(){const s=state.stats;return `<div class="heading"><div><h1>Store once. Reference everywhere.</h1><p>InMyArtifact is a local content-addressed store. Identical bytes resolve to the same SHA-256 blob while retaining separate bounded metadata records.</p></div><button class="primary" data-view="upload">Upload artifact</button></div><div class="metrics"><div class="metric"><strong>${s.records||0}</strong><span>Records</span></div><div class="metric"><strong>${s.uniqueBlobs||0}</strong><span>Unique blobs</span></div><div class="metric"><strong>${s.dedupHits||0}</strong><span>Dedup hits</span></div><div class="metric"><strong>${Math.round((s.physicalBytes||0)/1024)} KB</strong><span>Physical bytes</span></div></div><div class="grid"><div class="panel"><h2>Content identity</h2><p>Every blob is addressed by its SHA-256 content digest and exposed as an artifact://sha256/... reference.</p></div><div class="panel"><h2>Opaque storage</h2><p>No archive extraction, package install, parsing or execution occurs in the artifact service.</p></div><div class="panel"><h2>Deduplicated</h2><p>Uploading identical bytes creates another metadata record without duplicating the underlying blob.</p></div><div class="panel"><h2>Prepared integration</h2><p>Future Sandbox/Cache/R&D references are documented, but all ecosystem runtime calls remain off.</p></div></div>`}function artifacts(){return `<div class="heading"><div><h1>Artifacts</h1><p>Local metadata records backed by content-addressed immutable blobs.</p></div><button class="primary" data-view="upload">Upload</button></div>${state.items.length?`<div class="grid">${state.items.map(a=>`<article class="card"><span class="status">${esc(a.mediaType)}</span><h2>${esc(a.name)}</h2><div class="key">${esc(a.digest)}</div><p>${a.size} bytes · ${new Date(a.createdAt).toLocaleString()}</p><p class="key">${esc(a.ref)}</p><div class="actions"><a class="secondary" href="/api/artifacts/${a.id}/content">Download</a><button class="secondary verify" data-id="${a.id}">Verify</button><button class="danger delete" data-id="${a.id}">Delete record</button></div></article>`).join('')}</div>`:'<div class="empty">No artifacts stored.</div>'}`}function upload(){return `<div class="heading"><div><h1>Upload artifact</h1><p>Uploads are bounded to ${(state.health.maxUploadBytes/1024/1024).toFixed(0)} MB in this standalone foundation.</p></div></div><div class="grid"><form id="form" class="panel form"><label>File<input name="file" type="file" required class="filebox"></label><button class="primary">Store artifact</button></form><div class="panel"><h2>Result</h2><div id="out" class="code">No upload yet.</div></div></div>`}function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render()});document.querySelectorAll('.verify').forEach(b=>b.onclick=async()=>{try{const x=await api(`/api/artifacts/${b.dataset.id}/verify`,{method:'POST'});note(x.ok?'Digest verified':'Digest mismatch');await load()}catch(e){note(e.message)}});document.querySelectorAll('.delete').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this metadata record? The blob is deleted only when it is the last reference.'))return;try{await api(`/api/artifacts/${b.dataset.id}`,{method:'DELETE'});note('Artifact record deleted');await load()}catch(e){note(e.message)}});const f=$('#form');if(f)f.onsubmit=async e=>{e.preventDefault();const file=new FormData(f).get('file');try{const r=await fetch('/api/artifacts',{method:'POST',headers:{'content-type':file.type||'application/octet-stream','x-artifact-name':file.name},body:file});const x=await r.json();if(!r.ok)throw new Error(x.error);$('#out').textContent=JSON.stringify(x,null,2);note(x.deduplicated?'Existing blob reused':'Artifact stored');await load(false)}catch(err){$('#out').textContent=err.message}}}function render(){const v={overview,artifacts,upload};$('#app').innerHTML=shell(v[state.view]());bind()}async function load(renderNow=true){[state.items,state.stats,state.health]=await Promise.all([api('/api/artifacts'),api('/api/stats'),api('/api/health')]);if(renderNow)render()}load().catch(e=>{$('#app').textContent=e.message});