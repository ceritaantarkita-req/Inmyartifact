{
  "name": "inmy-artifact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check lib/artifact.mjs && node --check lib/http.mjs && node --check lib/integration.mjs && node --check lib/resolver.mjs && node --check lib/resolver-registry.mjs && node --check lib/store.mjs && node --check server.mjs",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "qa": "npm run check && npm test && npm run smoke"
  }
}
